<?xml version="1.0" encoding="UTF-8"?>
<!--DTD generated by XML Spy v3.5 NT (http://www.xmlspy.com)-->
<!ELEMENT COLUMN EMPTY>
<!ATTLIST COLUMN
	name CDATA  #REQUIRED
	localname CDATA  #REQUIRED
	dbtype (VARCHAR | VARCHAR2 | DATE | INTEGER | DOUBLE | CHAR | SMALLINT | TIMESTAMP | TIME | REAL | BIGINT | BIT | OBJECT | DECIMAL | BLOB ) #REQUIRED
>
<!ELEMENT DATABASE (TABLE+, DBTYPES?)>
<!--
A Database:
   name          is the name of the view inside the Java Enabling Tool (used i.e. in TABLE element). 
   databasename  is the name of the database inside the DBMS.
   user          is the name of the default user for accessing this database.
   password      is the password corresponding to the user.
   type          defines the DBMS used (DB2, ORACLE, ...)
   driver        is the driver to use for this database 
-->
<!ATTLIST DATABASE
	name CDATA #REQUIRED
>
<!-- !ATTLIST DATABASE
	databasename CDATA #REQUIRED
	user CDATA #REQUIRED
	password CDATA #REQUIRED
	name CDATA #REQUIRED
	type CDATA #REQUIRED
	driverJar CDATA #REQUIRED
	driver CDATA #REQUIRED
	jndidatasource CDATA #REQUIRED
-->
<!ELEMENT DBTYPE EMPTY>
<!--
TO BE REMOVED do not use
-->
<!ATTLIST DBTYPE
	dbtype (VARCHAR | VARCHAR2 | DATE | INTEGER | DOUBLE | CHAR | SMALLINT | TIMESTAMP | TIME | REAL | BIGINT | BIT | OBJECT | DECIMAL ) #REQUIRED
	java-type (Boolean | Byte | Char | Date | Double | Float | Integer | Long | Object | Short | String | boolean | byte | char | double | float | int | long | short | java.math.BigDecimal | java.util.Date ) #REQUIRED
>
<!ELEMENT DBTYPES (DBTYPE+)>
<!ATTLIST DBTYPES
	descriptif CDATA #REQUIRED
>
<!ELEMENT TABLE (COLUMN+)>
<!--
A database table:
   name    is the name of the table inside the Java Enabling Tool (used i.e. in MAPPING element). 
   localname         refers to a DATABASE element.
-->
<!ATTLIST TABLE
	name CDATA  #REQUIRED
	localname CDATA  #REQUIRED
>
