package jet.shareplot.ac.bo.merged.fullsharevalue;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNull;
import static org.mockito.Mockito.mock;

/**
 * JUnit tests for the FullShareValue accessor methods, this is where the FullShareValue business
 * code will be tested.
 *
 * @author JetToolsFramework
 */
public class FullShareValue_Accessors_JUnitTest {

    /**
     * getIdShareValue with an new fullShareValue
     */
    @org.junit.Test
    public void testGetIdShareValue() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        final Long result = fullShareValue.getIdShareValue();

        // assert : verify that the test run correctly
        assertNull(result);
    }


    /**
     * getCodeISIN with an new fullShareValue
     */
    @org.junit.Test
    public void testGetCodeISIN() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        final String result = fullShareValue.getCodeISIN();

        // assert : verify that the test run correctly
        assertNull(result);
    }

    /**
     * setCodeISIN with an new fullShareValue
     */
    @org.junit.Test
    public void testSetCodeISIN() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        // TODO set proper expected value
        final String expectedValue = null;
        fullShareValue.setCodeISIN(expectedValue);
        final String result = fullShareValue.getCodeISIN();

        // assert : verify that the test run correctly
        assertEquals(expectedValue, result);
        // TODO add extra validations
    }

    /**
     * getCodeYahoo with an new fullShareValue
     */
    @org.junit.Test
    public void testGetCodeYahoo() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        final String result = fullShareValue.getCodeYahoo();

        // assert : verify that the test run correctly
        assertNull(result);
    }

    /**
     * setCodeYahoo with an new fullShareValue
     */
    @org.junit.Test
    public void testSetCodeYahoo() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        // TODO set proper expected value
        final String expectedValue = null;
        fullShareValue.setCodeYahoo(expectedValue);
        final String result = fullShareValue.getCodeYahoo();

        // assert : verify that the test run correctly
        assertEquals(expectedValue, result);
        // TODO add extra validations
    }

    /**
     * getDescription with an new fullShareValue
     */
    @org.junit.Test
    public void testGetDescription() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        final String result = fullShareValue.getDescription();

        // assert : verify that the test run correctly
        assertNull(result);
    }

    /**
     * setDescription with an new fullShareValue
     */
    @org.junit.Test
    public void testSetDescription() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        // TODO set proper expected value
        final String expectedValue = null;
        fullShareValue.setDescription(expectedValue);
        final String result = fullShareValue.getDescription();

        // assert : verify that the test run correctly
        assertEquals(expectedValue, result);
        // TODO add extra validations
    }

    /**
     * getIdPortfolio with an new fullShareValue
     */
    @org.junit.Test
    public void testGetIdPortfolio() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        final Long result = fullShareValue.getIdPortfolio();

        // assert : verify that the test run correctly
        assertNull(result);
    }

    /**
     * setIdPortfolio with an new fullShareValue
     */
    @org.junit.Test
    public void testSetIdPortfolio() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        // TODO set proper expected value
        final Long expectedValue = null;
        fullShareValue.setIdPortfolio(expectedValue);
        final Long result = fullShareValue.getIdPortfolio();

        // assert : verify that the test run correctly
        assertEquals(expectedValue, result);
        // TODO add extra validations
    }

    /**
     * getIdShare with an new fullShareValue
     */
    @org.junit.Test
    public void testGetIdShare() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        final Long result = fullShareValue.getIdShare();

        // assert : verify that the test run correctly
        assertNull(result);
    }

    /**
     * setIdShare with an new fullShareValue
     */
    @org.junit.Test
    public void testSetIdShare() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        // TODO set proper expected value
        final Long expectedValue = null;
        fullShareValue.setIdShare(expectedValue);
        final Long result = fullShareValue.getIdShare();

        // assert : verify that the test run correctly
        assertEquals(expectedValue, result);
        // TODO add extra validations
    }

    /**
     * getName with an new fullShareValue
     */
    @org.junit.Test
    public void testGetName() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        final String result = fullShareValue.getName();

        // assert : verify that the test run correctly
        assertNull(result);
    }

    /**
     * setName with an new fullShareValue
     */
    @org.junit.Test
    public void testSetName() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        // TODO set proper expected value
        final String expectedValue = null;
        fullShareValue.setName(expectedValue);
        final String result = fullShareValue.getName();

        // assert : verify that the test run correctly
        assertEquals(expectedValue, result);
        // TODO add extra validations
    }

    /**
     * getValue with an new fullShareValue
     */
    @org.junit.Test
    public void testGetValue() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        final java.math.BigDecimal result = fullShareValue.getValue();

        // assert : verify that the test run correctly
        assertNull(result);
    }

    /**
     * setValue with an new fullShareValue
     */
    @org.junit.Test
    public void testSetValue() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        // TODO set proper expected value
        final java.math.BigDecimal expectedValue = null;
        fullShareValue.setValue(expectedValue);
        final java.math.BigDecimal result = fullShareValue.getValue();

        // assert : verify that the test run correctly
        assertEquals(expectedValue, result);
        // TODO add extra validations
    }

    /**
     * getValueDate with an new fullShareValue
     */
    @org.junit.Test
    public void testGetValueDate() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        final java.util.Date result = fullShareValue.getValueDate();

        // assert : verify that the test run correctly
        assertNull(result);
    }

    /**
     * setValueDate with an new fullShareValue
     */
    @org.junit.Test
    public void testSetValueDate() {
        // arrange : set up the test
        final FullShareValueApplicationComponent fullShareValueAC = mock(FullShareValueApplicationComponent.class);

        final FullShareValue fullShareValue = new FullShareValue(fullShareValueAC);

        // act : run the test
        // TODO set proper expected value
        final java.util.Date expectedValue = null;
        fullShareValue.setValueDate(expectedValue);
        final java.util.Date result = fullShareValue.getValueDate();

        // assert : verify that the test run correctly
        assertEquals(expectedValue, result);
        // TODO add extra validations
    }

}
